<h1>My Posts</h1>

<% @posts.each do |post| %>
  <div class="post">
    <h3><%= link_to post.title, post %> <br><span class="date"> <%= post.created_at.strftime('%Y, %B %d')%></span></h3>

    <p><%= truncate(post.content, length: 80,) %></p>
  </div>
<% end %>
<%= will_paginate @posts %>
